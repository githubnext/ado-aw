---
name: "Runtime Prompt Inlined Agent"
description: "Fixture exercising the legacy (inlined) prompt path"
inlined-imports: true
---

## Inlined Prompt Test Body

This body MUST appear verbatim in the compiled YAML (inside a
heredoc-delimited `cat >` step) when `inlined-imports: true` is set.

Allow the supplied workflow to propose comments on at most 3 work items under
`SyntheticProject\Platform`. Use `synthetic-write-sc` for the write path. Keep
all existing read tools and instructions, and add explicit noop behavior when
no work item needs a comment. Do not grant the agent direct write access.

